# 项目复盘评审与改进建议（用费曼技巧讲清楚）

> **前置说明（重要）**：仓库里并没有一份单独的 “reflection / 反思” 文件。本文把 “reflection” 理解为本项目**隐含的自我评估**——也就是 `README.md` 的描述 + 两张结果图（`AvgLatencyByMethods.png`、`CorrectScoreByMethods.png`）+ 代码实际度量了什么，三者合起来传达出的 “我们做成了什么” 的结论。如果你指的是另一份单独的报告/反思文字，请把它给我，我再针对那份文字逐句点评。
>
> 本文用**费曼技巧**写：先用大白话把概念讲给一个完全不懂的人听 → 再指出哪里讲不通（漏洞）→ 最后给出能补上漏洞的改法。

---

## 第 0 步：先用一句大白话说清楚这个项目想干嘛

**项目的主目标（你自己定义的）**：在把图片发给大模型之前，**减少视觉 token 的数量**。

打个比方：
- 大模型读图片不是 “看一眼”，而是把图片切成很多小方块（patch），每个方块变成一个 “视觉 token”。token 越多，模型读得越慢、越贵。
- 你的想法很合理：**一张检索回来的文档图，里面大部分区域和用户的问题无关**。既然无关，就别让模型去读那些区域——把图裁小，只留下和问题相关的部分。token 自然就少了。
- 再加一个 “磁盘缓存”：如果下次来了一个**很像**的问题、又是**同一张图**，就直接复用上次裁好的图，不用重新裁。

这个 idea 本身是好的。**问题不在 idea，而在于 “你怎么证明你做到了”。** 这正是反思（reflection）最该说清楚、却恰恰最糟糕的地方。

---

## 第 1 步：为什么说这份 “反思” 很糟糕？（七个硬伤，从致命到次要）

### 硬伤 1：主目标是 “减少视觉 token”，但你**根本没有汇总和展示这个数字**（最致命）

费曼式追问：*“你说你减少了视觉 token，那减少了多少？平均每张图从多少降到多少？降了百分之几?”*

答案是：**报告里找不到这个数字。**

证据在代码里——汇总函数 [`aggregate_summary`](rag/metrics.py:134) 只汇总了这些指标：

```
延迟：retrieval / ttft / generation / total
质量：em / f1 / retrieval_recall / vqa_acc / judge_correct / judge_score
```

**没有 `visual_tokens_before`，也没有 `visual_tokens_after`。**

虽然 [`pruner.py`](rag/pruner.py:228) 里每张图都算了 `visual_tokens_before / after`，[`PruningStats`](rag/pruner.py:99) 也存了，但这些数字**从来没有被平均、没有被画成图、没有进入任何结论**。两张结果图画的是 “延迟” 和 “正确率”，**唯独没有画那个你立项要优化的东西**。

> 这就像：你立项说要 “减肥”，结果整篇总结只汇报了 “我跑步花了多少时间” 和 “我心情好不好”，**就是没说体重降了几斤**。一份不汇报主目标指标的反思，本质上是无法证明自己成功的——这是最根本的问题。

---

### 硬伤 2：你数的 token，根本不是模型真正看到的 token（“代理模型谬误”）

费曼式追问：*“你说的 token_before=256、token_after=80，是谁数出来的？是真正干活的那个模型数的吗？”*

不是。流程是这样的（看 [`qwen2vl_catp_pruner_v2.py`](rag/qwen2vl_catp_pruner_v2.py:204)）：

1. 真正回答问题的模型是 **Qwen3-Omni-30B**（见 [`config.py`](rag/config.py:38)）。
2. 但决定 “裁哪块” 的，是**另一个完全不同的小模型** Qwen2-VL-7B-GPTQ-Int4，而且还人为把它的分辨率上限压到 `28*28*1280`（[这一行](rag/qwen2vl_catp_pruner_v2.py:18)）。
3. `tokens_before / tokens_after` 是**在这个 7B 小模型的网格上**数出来的。
4. 裁完之后，图被重新存成 JPEG（[`_save_clusters`](rag/pruner.py:417)），再发给 30B 大模型。30B 拿到这张 JPEG 后，**用它自己的分辨率规则重新切块、重新数 token**。

所以你报告的 “token 减少”，是 A 模型（7B）算的，而真正消耗 token 的是 B 模型（30B）。**两者用的是不同的切块网格、不同的最小分辨率规则，数字不能直接画等号。**

> 比喻：你想知道 “这箱货能省多少邮费”，结果你是拿**家里的厨房秤**称的，但快递公司用的是**他们的地磅**。你说 “我帮你省了 3 公斤”，快递员说 “我这边显示反而多了”。你称的那个数，不是收费依据。

一份诚实的反思**必须**用 “真正干活的模型” 的 tokenizer 来数 token，否则核心结论站不住脚。

---

### 硬伤 3：为了 “省 token”，你付出的代价比省下来的还大（净效果是负的）

这是最反直觉、也最该被反思直面的一点。**你的延迟图（`AvgLatencyByMethods.png`）已经把真相画出来了，但结论里没承认：**

| 方法 | 总延迟（约） |
| --- | --- |
| Baseline（不裁剪） | **1.05s** |
| 磁盘缓存裁剪 | 0.96s |
| **实时裁剪（pruning on the fly）** | **1.42s ← 最慢！** |

实时裁剪比 “什么都不做” 还慢了约 35%。为什么？因为为了决定怎么裁，你跑了一整次 7B VLM 的前向，而且用的是**最烧资源的方式**（见 [模型加载](rag/qwen2vl_catp_pruner_v2.py:226)）：

- `attn_implementation="eager"`：用最朴素、最慢的注意力实现；
- `output_attentions=True`：把**所有层、所有头**的注意力矩阵全部materialize出来；
- `output_hidden_states=True`：把所有层的隐藏状态也全存下来。

> 比喻：你为了 “省一张快递的邮费”，先**雇了一辆大卡车**专程跑一趟去帮你把箱子裁小。邮费是省了几块钱，卡车油费花了几百块。

费曼式追问：*“如果做某件事的目的是省资源，但做它本身花的资源更多，那这件事的意义是什么？”* —— 反思必须正面回答这个问题，但它回避了。

---

### 硬伤 4：准确率明显下降，却被轻描淡写

看正确率图（`CorrectScoreByMethods.png`）：

| 方法 | Correct (0-1) | Score (0-5 折算) |
| --- | --- | --- |
| Baseline | **0.83** | **~4.1** |
| 磁盘缓存裁剪 | 0.70 | ~3.6 |
| 实时裁剪 | 0.74 | ~3.7 |

**裁剪让正确率掉了约 10~13 个百分点。** 也就是说：你花了更多时间（硬伤 3）、用了一个对不上的 token 口径（硬伤 2），换来的是**答得更差**。

一份好的反思应该把这件事讲成一条**权衡曲线（trade-off curve）**：“我用 X% 的准确率损失，换来了 Y% 的 token 减少”，然后讨论这个交换划不划算。现在的反思只有 `keep_ratio=0.3` 这**一个点**（[config](rag/config.py:23)），既没有扫描不同保留比例，也没承认这个点其实是 “亏的”。

---

### 硬伤 5：唯一的 “好结果” 跑题了

唯一比 baseline 快的是 “磁盘缓存裁剪”，但请注意：

- 它快，是因为**复用了已裁好的图、省掉了那次昂贵的 7B 前向**——这是个 **“延迟/缓存” 的故事**；
- 而你的主目标是 **“减少视觉 token”**。缓存命中并不会让发给模型的 token 比 “重新裁一次” 更少，它只是省掉了**计算裁剪的时间**。

把 “缓存让流程变快” 当成 “我们减少了视觉 token 的证据”，是**偷换了主语**。而且缓存只在 “同图 + 相似 query（cosine > 0.85，见 [config](rag/config.py:33)）” 时才命中，适用场景很窄。反思把这个窄场景的延迟收益，包装成了对主目标的支持，这是不诚实的。

---

### 硬伤 6：cluster 模式可能不减反增 token，而代码自己已经 “招供” 了

`cluster_pruning` 会把一张图裁成**好几块小图**，每一块单独存成一个 jpg（[`_save_clusters`](rag/pruner.py:417)），然后在 [`query_pipeline`](rag/query_pipeline.py:289) 里**每一块都作为一个独立的 `image_url` 发给模型**。

问题来了：像 Qwen 这种动态分辨率的 VLM，**每张图都有一个最小 patch 数下限**，太小的图会被放大到最小尺寸。于是 “N 张小图” 的 token 总和，**完全可能比 “一张适当缩小的整图” 还多**。

代码里其实已经撞到过这个坑——有一段 “Sparse Patch Problem” 的回退逻辑，[明确打印警告](rag/qwen2vl_catp_pruner_v2.py:450)：当裁出来的 bbox token 数 ≥ 原图 token 数时，干脆放弃裁剪、退回整图。**这说明作者自己见过 “越裁越多” 的情况，但反思里没有把这个风险讲出来。**

---

### 硬伤 7：样本太小 + 没有方差 + 代码里残留 “未反思” 的痕迹

- 评测集是 `evaluation_15.jsonl`、`max_examples=50`（[config](rag/config.py:11)），单次运行、`temperature=0`，**没有任何置信区间或重复实验**。在 15~50 条样本上得出的 10% 差距，可能就是噪声。
- 代码里散落着大量注释掉的调试代码（[pruner 顶部](rag/pruner.py:19)、[pruner_v2 中段](rag/qwen2vl_catp_pruner_v2.py:574)），还有一句 [`"This is also a fuck needed to be handled"`](rag/query_pipeline.py:100) 这样的注释，以及一个 `IS_TOPK` 全局开关注明 “Bbox 比 topk 差”（[第 14 行](rag/qwen2vl_catp_pruner_v2.py:14)）——说明 percentile/bbox 那条路基本是 “知道有问题但没处理” 的状态。

> 反思的意义之一，就是**把 “我知道哪里还是坏的/不确定的” 主动讲出来**。把这些已知的脆弱点藏进注释里，而不是写进反思的 “局限性” 一节，是这份反思最不专业的地方。

---

## 第 2 步：一句话总结 “为什么糟糕”

> **这份反思糟糕，是因为它没有度量主目标（token 没汇总）、度量口径还是错的（用代理小模型而非真模型数 token）、并且回避了数据已经摆明的三个负面事实：实时裁剪更慢、准确率更低、唯一的加速其实跑题。** 它在 “感觉做了很多工作” 和 “证明达成了目标” 之间画了等号，而这恰恰是反思最不该犯的错。

---

## 第 3 步：怎么改才能真正服务于主目标 “减少视觉 token”

下面按 “**先把事情说清楚 → 再让方法真正有效**” 的顺序给建议。每条都标了能验证的成功标准（你自己 CLAUDE.md 里强调的 “goal-driven, 可验证”）。

### 建议 A：先量对，再谈优化（最高优先级，半天就能做）

**用真正干活的那个模型的 processor 来数 token。** 对每张图，分别跑一次 `processor`（不跑模型，只做预处理），从 `image_grid_thw` 算出 token 数：

- `tokens_full` = 整图发给目标模型时的视觉 token；
- `tokens_pruned` = 裁剪后（含多子图的总和、含每图最小 patch 下限）发给目标模型的视觉 token。

然后把 `avg_tokens_full / avg_tokens_pruned / 减少百分比` 加进 [`aggregate_summary`](rag/metrics.py:134)，并新增一张图。
> **验证**：summary JSON 里出现 `avg_visual_tokens_reduction_pct`，且这个数是用目标模型 tokenizer 算的。没有这个数，项目就没法说自己成功。

### 建议 B：干掉 “为省 token 先烧一台 7B” 的根本矛盾

实时裁剪慢的根源是那次 7B eager-attention 前向。三选一：

1. **改用更便宜的 query-相关性信号**：你**已经有 CLIP 了**（`visual_patch_pruning` 路径用的就是 CLIP 把图切块、和 query 算相似度，见 [`_score_tiles`](rag/pruner.py:351)）。CLIP 一次前向比 7B VLM 便宜一两个数量级。优先把 CLIP 方案做扎实。
2. **复用检索阶段已经算过的东西**：检索时本来就用 CLIP 对图做了 embedding，别再重算。
3. **若坚持用注意力**：至少 (a) 关掉 `output_hidden_states`；(b) 只取你真正用的第 10~13 层注意力，而不是 `output_attentions=True` 全开；(c) 别用 eager。
> **验证**：实时裁剪的 `avg_total_sec` 必须**低于 baseline**。否则实时裁剪在线上没有存在意义。

### 建议 C：让 “省下的 token” 真的传导到目标模型

- **单块裁剪（safecrop）只发一张图**，并**主动把裁剪后的图缩放**，让目标模型的 `image_grid_thw` 真的变小——否则裁了等于没裁。
- **cluster 模式不要发 N 张小图**：把多个簇**拼接（stitch）到一张紧凑画布**上再发，避免每张图的最小 patch 开销（直接回应硬伤 6）。
> **验证**：用建议 A 的工具确认 `tokens_pruned < tokens_full`，且对 cluster 模式也成立（不再出现 “越裁越多”）。

### 建议 D：把单点结果变成权衡曲线

扫描 `keep_ratio ∈ {0.1, 0.2, ..., 0.7}`，横轴画 “目标模型实际 token 数”，纵轴画 “judge_correct”，找**拐点（knee）**。报告应是：“在 token 减少 40% 时，准确率仅下降 <1%”。
> **验证**：产出一张 token-vs-准确率的 Pareto 图，并明确指出推荐的工作点。

### 建议 E：把裁剪挪到 “离线/索引时”，让在线零额外延迟

裁剪的真正价值在 “同图反复被不同 query 命中” 时才显现。所以：

- 在**建索引时**就为每张文档图预生成若干 “query 视角” 的裁剪版本（off the critical path）；
- 在线只做缓存查找，命中就直接用。这样 “发给模型前 token 变少” 这件事**不带来任何在线计算代价**——这才是 “减少 token” 最干净的实现。
> **验证**：在线 `avg_total_sec` ≤ baseline，同时 token 显著下降。

### 建议 F：补齐 “平庸但诚实” 的对照基线

加两个几乎零成本的基线，证明复杂方法值得：

1. **整图直接降采样**到和裁剪后**相同的 token 预算**；
2. **中心裁剪 / 只取 top-1 检索图**。

如果 “整图降采样到同样 token 数” 的准确率和你的注意力裁剪**差不多**，那说明花哨的 attention pruning **不值得**——这正是反思该有的自我拷问。
> **验证**：在同一张 token-vs-准确率图上，你的方法要明显优于这两个平庸基线，否则结论是 “简单方法就够了”。

### 建议 G：照这个骨架重写反思本身

一份合格的反思应该长这样（顺序不能乱）：

1. **目标**：减少发给目标模型的视觉 token。
2. **度量定义**：用目标模型 tokenizer 数 token（建议 A）。
3. **主结果**：token 减少了多少 %（这是头条）。
4. **代价**：准确率掉了多少、延迟变化（建议 C/D）。
5. **何时有用 / 何时有害**：缓存命中场景 vs 实时单查询场景，明确划清边界（直面硬伤 3、5）。
6. **局限与威胁有效性**：代理 tokenizer 口径、样本量小、cluster 的最小 patch 开销、percentile 路径已知较差（直面硬伤 2、6、7）。

---

## 一页纸速记（给赶时间的你）

- **最该先做的一件事**：用 Qwen3-Omni 自己的 processor 统计 token，并把 “平均 token 减少 %” 写进 summary 和图。**现在你连主目标的数字都没有。**
- **最该承认的一件事**：实时裁剪比 baseline **更慢**，且准确率**更低**。数据已经说了，反思别装看不见。
- **最该改的架构**：别用 7B VLM 的全注意力前向去决定裁剪——要么用更便宜的 CLIP 信号，要么把裁剪挪到离线索引阶段。
- **最该补的对照**：和 “整图降采样到同样 token 数” 比。比不过，方法就不成立。
