# Query-Aware Visual Token Pruning and Disk Cache in Multimodal RAG

Prune and store the retrieved images based on user queries. If similar queries ask for the same images before, we use the pruned images stored in the disk. Thus, TTFT and the overal latency decrease.


